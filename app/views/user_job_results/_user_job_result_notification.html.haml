%div.toast{"data-controller": "user-job-result-notification"}
  .toast-header{class: "bg-#{user_job_result.status == 'success' ? 'info' : 'danger'}"}
    %strong= t(".job_#{user_job_result.status}")
  .toast-body
    = user_job_result.name
